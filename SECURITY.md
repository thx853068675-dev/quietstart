# 隐私与安全

轻启在手机本地保存应用名称、规则、设置和运行记录，OCR 在本机执行。0.9.41 已移除云订阅、HTTP 更新客户端和服务器信任配置，应用运行不依赖外网服务。

网络权限仍用于本机 HDC 激活连接，目标固定为 `127.0.0.1`。系统无线调试仍需开启，部分系统要求连接 Wi-Fi；不需要外网可达。本机激活身份属于私有数据，不得公开。

识别条件随应用内置，可在手机手动编辑、恢复默认。升级优先保留个人修改，否则把旧版已下载的生效条件迁为本机条件；成功保存后清除旧订阅元数据。新广告规则仍需用户确认。

开发环境下载依赖、开发者主动下载社区测试样本会访问网络，这些工具不进入应用安装包。

不要公开签名配置、私钥、设备序列号、完整私人页面布局或个人运行记录。发现凭据暴露时撤销对应凭据；仓库尚未配置私密报告渠道，请勿在公开 Issue 中贴密钥。
