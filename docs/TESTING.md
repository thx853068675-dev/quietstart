# 测试

开源整理回归：0.9.43 离线套件共 418 项通过，包含源码导出过滤、凭据检测、安装目标选择和安装失败处理。Windows 批处理仅做静态审阅，未在 Windows 真机运行。

0.9.42 单包安装使用 local-activation、worker-installer 和 hdc-protocol 测试，覆盖安装失败不得启动、同连接收发顺序、首次安装、已有模块和损坏传输；真机范围见 [单包安装验证](single-package-0.9.42.md)。

回归工具使用 DevEco 随附的 TypeScript 转译器与 Node.js 测试运行器。需要先安装 DevEco，默认 macOS 路径可用 `DEVECO_APP` 覆盖。

```sh
python3 tools/run-tests.py
```

上面的基础回归不读取社区原始快照。完整社区回放需主动下载公开来源数据：

```sh
python3 tools/community/fetch-reviewed.py
python3 tools/run-tests.py --community
```

下载工具只接受来源清单中的 URL 和 SHA-256，不提取图片；网络失败或内容变化时停止。原始数据保存在 Git 忽略目录中，不属于项目 MIT 授权内容。不要未经审阅把真实手机全量布局、截图或社区原件提交到仓库。

0.9.40 的 348 项数字对应 worker、community-replay、community-structural 三个测试文件，并非所有测试总数。离线测试不证明真机时延、耗电或广告覆盖率；缺失 enabled、层次信息的模拟实验单独统计。

## 0.9.52 监督与退出诊断

完整 Node 套件（含已校验来源的社区快照）582 项通过；新增恢复时不重复打开页面、首次连接仍显示界面，以及后台切换不影响普通打开/耗尽提示/主动停止的检查。生产监督脚本另有 6 项离线场景测试，覆盖健康、休眠/挂起、进程丢失、RPC 假在线、3 次重试上限、退避中停止和重复/残留锁。运行：

```sh
python3 tests/supervisor.test.py
node --test tests/session-health.test.cjs
```

构建通过；真机注入方法、已验证结果和未验证边界见[后台生存验证](LIFECYCLE.md)。模型测试不替代 OS 6 实测或长期能耗测试。

## 0.9.51 骨架背景恢复

修复存在图层信息时隐藏骨架的条件：装饰骨架常驻，广告位置标记最后绘制。主包及内嵌模块构建通过，Pura X / HarmonyOS 7 已更新并恢复本机连接。大麦带 4 项图层的手动规则已通过真机截图确认两条骨架线可见、圆环和位置标记保留。更新前后 32 条规则、规则决定和设置一致；本轮仅做构建与界面验证。

## 0.9.50 规则卡片操作布局

主包及内嵌模块构建通过。Pura X / HarmonyOS 7 更新主包后，本机连接自行完成模块准备并恢复在线。真机双向滑动大麦规则，顶部标识随当前规则在 `S08 · 手动学习`、`S02 · 自动识别` 之间切换，页码、规则内容及操作目标一致。策略标识在卡片水平中心，与应用名称同一行；删除按钮为 42 vp 圆形，与启用／停用按钮等高对齐。两页均验证了删除确认弹窗并取消，更新前后 32 条规则、规则决定和设置内容一致。本轮为界面调整，未重复运行识别与点击套件。

## 0.9.49 横排按钮示意

按钮展示与位置投影的 30 项定向回归通过，主包及内嵌模块构建通过。芒果 TV 横排文字示意的背景高度由 44 vp 缩至 26 vp，保留左右顺序与可读字号；圆环与上下排列保持等比投影。

Pura X / HarmonyOS 7 真机已安装并恢复本机连接，核对[芒果 TV 规则卡片](images/mgtv-rule-949.jpeg)：文字居中且完整包含在背景内。更新前后 32 条规则、规则决定和设置内容一致。本轮仅调整展示，未重复运行广告识别、点击或强化学习流程。

## 0.9.48 位置修正、规则布局与强化学习

离线基础套件 **471 项全部通过**，主包与内嵌工作模块构建通过。新增回归覆盖可见图片与点击容器的区别、子控件遮挡／禁用／不唯一、手动规则复用、大麦 Circle 轮廓、芒果 TV 分离数字的背景范围、历史样式补充，以及手动学习成功／失败返回、超时与结果确认。可选社区原始快照套件未运行。

Pura X / HarmonyOS 7 真机：

- 12306 修正前后各冷启动 4 轮，分别捕获 1 次和 2 次开屏广告。可见图片按钮为 `[1068,158]–[1212,233]`，原容器为 `[960,117]–[1320,390]`。原落点 `(1140,254)` 在图片下方，新落点 `(1140,196)` 在图片内。修正后的两次均收到输入完成与连续两帧消失确认，确认阶段约 1.0／1.1 秒；原记录约 3.6 秒，不能据此证明点击原来有效。采样工具可能影响耗时，这不是性能基准或全部广告样式覆盖率。
- 大麦强化学习：前台页面打开目标应用，收到广告“跳过”的实际控件点击事件后保存并直接启用手动规则，自动返回应用管理并显示“学习成功”；原规则保留。另一次不点击，60 秒后自动返回并显示超时原因。首次打开可能出现系统确认，需要选择“打开”。
- 仅从电脑更新主包；工作模块由轻启本机连接自行更新、恢复会话心跳，同版本多次覆盖均结束安装状态。没有通过电脑手动安装内部模块。

原始真机布局、广告截图、运行记录保存在忽略目录 `dist/diagnostics-0.9.48/`，不随源码分发。使用说明仅附轻启自身界面截图。

## 0.9.47 策略编号与按钮示意

离线基础套件 **461 项全部通过**，主包及内嵌工作模块构建、版本和完整性检查通过。新增 `recognition-profile.test.cjs` 验证 S01～S08、主策略与辅助证据、纯中文/数字/混合/图标、计时左右及上下分离、圆环内文字、用户自定义用词、明确外形与未知默认圆角矩形。坐标投影验证位置关系和边界，不把外观分类当作点击授权。

回放使用已有的下厨房、驾校一点通、芒果 TV、大麦、12306 和爱奇艺脱敏控件样本；编号覆盖中也包含构造场景。`worker.test.cjs` 增加旧规则补充展示信息后审批、统计和点击行为不变，以及损坏展示字段不会删除基础规则的用例。新增 `7x24`、尺寸/乘法及旧审批的反例，修复数字夹着叉号被误当关闭按钮的问题。

Pura X / HarmonyOS 7：只安装主包，经本机连接自行更新工作模块后在线。大麦、芒果 TV、虎扑分别完成自动点击，保存的本地 profile 与原规则合并；芒果 TV 保留左侧计时和右侧“跳过”，大麦保留操作文字和父容器，虎扑本次实际出现的是明确“跳过广告”文字。首次三次测试均取得消失确认。通过真实规则卡片点击编号可直达对应策略说明。

本轮圆环、椭圆及其他明确外形主要通过构造布局和现有离线回归验证，没有声称真机触发了所有外形。系统常未提供圆角属性，按需求默认绘制圆角矩形；未增加截图或 OCR 来猜测外观。本机缺少 17 份需另行下载的社区原始快照，未执行依赖它们的可选 `--community` 套件。

## 0.9.46 安装恢复、计数与单条删除

离线套件 **438 项通过**，主包和内嵌模块构建及完整性检查通过。安装用例包括进程退出后的结果恢复、重复连接不重复传输、错误版本/缺失模块、超时、失败却返回 0、以及实际生成的安装脚本在模拟 `bm` 环境中的执行。模拟工具同时复现“普通文件重定向无输出、管道有输出”的真机行为。

Pura X / HarmonyOS 7 真机验证：

- 仅通过电脑更新主包，再在轻启点击本机连接。三次同版本工作模块覆盖均由轻启自行完成，安装后自动重新打开、核验回执并恢复新会话心跳；没有手动安装内部模块或补写回执。
- 结束会话后再次本机连接，复用了已核验模块，未进入传输/安装任务，约 5 秒恢复在线。
- 在大麦的两条原有规则旁添加一条未启用的临时测试规则，经真实界面删除后，卡片从 3 条回到 2 条，总数从 34 回到 33；原有规则均保留，临时测试数据已清理。
- 单条删除的离线用例覆盖兄弟规则和审批保留、旧写回及旧采样不能复活规则、删除发生于点击前时取消输入，以及后续新采样重新学习。

概览统计改用当前会话已发出点击的次数；消失确认仍是独立指标。统计组件已改为响应状态变化，避免显示旧会话首次渲染的数字；真机打开大麦触发一次点击后，概览从 0 更新为 1。安装失败/超时的完整覆盖主要来自离线脚本测试，本轮未重新清空设备进行首装和第三方重签。

## 0.9.45 主动学习与五款应用回归

离线套件 **433 项全部通过**。新增五份本机脱敏布局：`*-splash-0945.json`。只保留控件结构、状态、布局及广告关闭相关文字和标识；其他文字替换为“普通内容”，不含截图素材、设备标识或账号信息。它们是五个实际场景，不是广告覆盖率统计。

| 场景 | 修复的识别缺口 |
| --- | --- |
| 下厨房第二种样式、驾校一点通 | 不接收触摸的文字标签、同组裸数字计时、由紧凑父容器接收手势 |
| 芒果 TV | 分离的计时文字与跳过按钮、带分隔符的广告标记 |
| 大麦 | 圆形装饰与嵌套布局包围文字按钮；手动点击事件向顶部容器上报 |
| 铁路12306 | 无文字关闭控件与共享布局中广告内容图片的结构关联 |

2026-09-14，Pura X / HarmonyOS 7：这五款应用均取得过自动点击及连续两次目标消失确认。本轮发现前台至发起点击约 0.5～1.5 秒，包含启动等待，不能视为所有设备或广告的保证。12306 首轮目标消失但广告标记仍在，正确保留为“未能确认”；第二轮取得完整消失确认，未为了计数放宽验证。

大麦手动学习使用官方输入工具模拟用户真实点击，经过系统控件事件回调、点击前布局对应、两次消失验证，追加并直接启用手动规则，原自动规则保留。下一次打开由这条手动规则自动点击，规则页记录 1 次尝试、1 次确认消失。还在真实界面检查了启动/取消学习、来源筛选及设置开关。控件事件缺失、自绘按钮、无法唯一对应事件时仍可能无法手动学习，不会把未知点击直接保存。

`manual-learning.test.cjs` 验证真实布局回放、手动事件对应、事件向父容器上报及错误窗口/禁用/遮挡等反例。`worker.test.cjs` 验证追加不清空、默认审批、取消/过期/离开不保存；`reminder.test.cjs` 验证新规则通知固定 ID、权限/关闭竞争和不增加跳过次数。通知横幅展示仍由系统决定。

本次开发中反复覆盖同版本工作模块曾中断本机安装回执；最终调试安装主包及工作模块后，使用轻启本机连接取得新会话心跳。本轮没有重新完成清空环境后的单包首装、第三方重签或完全拔线测试，不能用本轮识别验证代替这些安装验证。

## 未报告点击能力的开屏按钮

`semantic-splash.test.cjs` 使用斗鱼真机布局的最小化样本，覆盖 `clickable=false` 的明确文字按钮、动态数字 ID、倒计时变化，以及禁用、遮挡、购买文字、候选重叠等反例。`worker.test.cjs` 验证待确认 → 启用 → 实时复核 → 点击 → 消失确认全流程。

样本 `tests/fixtures/semantic-splash-0943.json` 来自本机采集，仅保留控件类型、布局和状态，文字仅保留“跳过4”“广告”；不含截图和广告素材。新候选仍需广告或倒计时依据并由用户启用；已启用规则匹配实时语义、类型、位置及遮挡，不重复要求 OCR。此分支用于现代系统的布局快照识别路径。

0.9.43 已在 Pura X / HarmonyOS 7 上验证：仅更新主包，经轻启本机连接自行更新工作模块，斗鱼生成待确认规则；从规则页启用后重新启动斗鱼，发出点击并在两次后续快照中确认目标及广告标记消失。该次前台发现至发起点击 1299 ms，输入调用 108 ms，点击后确认 897 ms；这些是单次真机结果，不代表整体时延或所有厂商的手势控件均可点击。

## 预编译包重签

自动重签的独立离线测试（不读取真实签名材料、不连接手机）：

```sh
python3 tests/resign-hap.test.py
python3 tests/resign-xiaobai.test.py
```

真实签名与设备验证范围见[重签说明](RESIGN.md#验证范围)。
